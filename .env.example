# Anthropic. Used for scoring. Server side only.
ANTHROPIC_API_KEY=

# Optional. Defaults to claude-opus-5.
SCORING_MODEL=

# Supabase. The service role key is server side only and must never be prefixed NEXT_PUBLIC_.
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
